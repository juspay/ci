# ci

A Haskell playground for experiments around CI tooling.

## Roadmap

- [ ] Justfile introspection — dump the recipe graph reachable from `just ci` as filtered JSON ([#2](https://github.com/juspay/ci/pull/2)).
